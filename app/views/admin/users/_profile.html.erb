<div class="mx-auto"><%= image_tag user.show_p_img(100, 100) %></div>
<p class="mx-auto">ニックネーム:<%= user.nickname %></p>
<p class="mx-auto">名前：<%= user.name %></p>
<p class="mx-auto">email：<%= user.email %></p>

<p class="mx-auto">コメント</p>
<div class="mx-auto mb-5 w-50 text-break border border-3 rounded"><%= simple_format(user.show_comment, sanitize: true) %></div>


<p class="mx-auto">これまで投稿した数：<%= user.posts.count %>件</p>
<p class="mx-auto">これまで投稿へのコメントした数：<%= user.comments.count %>件</p>

<table class="container-fluid">
    <thead>
        <tr class="row-fluid table-border-l">
            <th class="col-5 table-border-s">
                <p>参加・参加希望中のグループリスト</p>
            </th>
            <th class="col-5">
                <p>説明</p>
            </th>
            <th class="col-2">
                <p>リンク</p>
            </th>
        </tr>
        </tr>
    </thead>
    <tbody>
        <% groups.each do |group| %>
        <tr class="row-fluid table-border-s">
            <td class="col-5 table-border-s">
                <p>・<%= group.title %></p>
            </td>
            <td class="col-5 table-border-s">
                    <p><%= group.body %></p>
                </td>
            <td class="col-2 table-border-s">
                <%= link_to admin_group_path(group.id) do %>
                    <p class="btn btn-info">移動</p>
                <% end %>
            </td>
        </tr>
        <% end %>
        <%= paginate groups %>
    </tbody>
</table>
